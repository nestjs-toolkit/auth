export enum RoleEnum {
  User = 'user',
  Webmaster = 'webmaster',
}

export enum PermissionEnum {
  PostRead = 'post.read',
  PostWrite = 'post.write',
  UserRead = 'users.read',
  UserWrite = 'users.write',
}
